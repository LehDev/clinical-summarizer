# ============================================================================
# requirements.txt - Fallback para pip (caso uv não esteja disponível)
#
# NOTA: Este arquivo é gerado manualmente como fallback.
# O arquivo principal de dependências é pyproject.toml
#
# Para instalar com pip:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt  # para desenvolvimento
# ============================================================================

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
psycopg[binary,pool]>=3.2.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
